//import ;
import org.junit.Test;
import static org.junit.Assert.*;

public class ApplicationTest {

    @Test
    public void main() {
        // Utilisation de Junit 4. + harmcrest (inutilisé)
        assertTrue(junit.runner.Version.id().startsWith("4."));
    }
}